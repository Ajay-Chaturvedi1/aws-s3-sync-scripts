# aws-s3-sync-scripts
S3 Sync Tools: syncFilesToBucket.sh: Uploads all local files or a specific arg to S3. Skips if the file exists in the bucket. getFileFromBucket.sh: Downloads a required arg from S3 only if not found locally.
